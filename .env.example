# Server Configuration
PORT=3000
NODE_ENV=development

# SMTP Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tagverse.iio@gmail.com
SMTP_PASS=axok nqva abmb zbnw
FROM_NAME="Your App Name"  # Fallback if config API is unavailable

# Config API
CONFIG_API_URL=https://your-config-api.com/config
CONFIG_API_KEY=your-config-api-key
CONFIG_CACHE_TTL=300000  # 5 minutes in milliseconds

# API Security
API_KEYS=your-api-key-1,your-api-key-2

# Logging
LOG_LEVEL=info
LOG_DIR=logs
